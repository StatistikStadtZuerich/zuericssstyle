# add_zcss_deps

add_zcss_deps

## Usage

``` r
add_zcss_deps(tag)
```

## Arguments

- tag:

  tag/tagList, e.g. a fluidPage

## Value

fluidPage/tag with ssz css dependencies added, including shiny widget
stylings

## Examples

``` r
if (FALSE) { # \dontrun{
inst / examples / ssz_deps_with_bootstrap / app.r
} # }
```
